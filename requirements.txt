torch>=2.2.0
torchvision>=0.17.0

transformers>=4.40.0
datasets>=2.19.0
accelerate>=0.29.0

codecarbon>=2.3.4

numpy>=1.26.0
pandas>=2.2.0

py-sudoku>=1.0.3

matplotlib>=3.8.0
seaborn>=0.13.0

wandb>=0.16.0
weave>=0.51.0           # LLM/ML observability — powers the wandb.ai/.../weave/monitors page

jupyter>=1.0.0
ipywidgets>=8.1.0

tqdm>=4.66.0
einops>=0.7.0
scipy>=1.13.0

argdantic>=0.6.0        # CLI argument parsing (wraps argparse with pydantic)
pydantic>=2.0.0         # data validation (used by argdantic)
huggingface_hub>=0.22.0 # downloading datasets/models from HuggingFace Hub

peft>=0.10.0            # LoRA / QLoRA for LLM fine-tuning
bitsandbytes>=0.43.0    # 4-bit quantization for QLoRA
pyyaml>=6.0             # YAML config loading
python-dotenv>=1.0.0    # load .env for machine-local config / secrets

# AdamATan2 is REQUIRED for the official TRM recipe (Less-is-More paper).
# We use lucidrains' pure-Python `adam-atan2-pytorch` — the PyPI `adam-atan2`
# package is only a wrapper around an unbundled CUDA kernel and is unusable
# on Windows / CPU-only setups. The trainer still prefers the CUDA build
# `adam_atan2.AdamATan2` if it's importable, otherwise uses the pure-Python
# implementation. It will hard-error (never fall back to AdamW) if neither
# is available.
adam-atan2-pytorch>=0.2.8

